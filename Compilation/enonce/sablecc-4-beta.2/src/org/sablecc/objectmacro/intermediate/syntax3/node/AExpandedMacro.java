/* This file was generated by SableCC (http://www.sablecc.org/). */

package org.sablecc.objectmacro.intermediate.syntax3.node;

import java.util.*;
import org.sablecc.objectmacro.intermediate.syntax3.analysis.*;

@SuppressWarnings("nls")
public final class AExpandedMacro extends PExpandedMacro
{
    private TString _name_;
    private final LinkedList<TString> _params_ = new LinkedList<TString>();
    private final LinkedList<TString> _ancestorRefs_ = new LinkedList<TString>();
    private final LinkedList<TString> _expands_ = new LinkedList<TString>();

    public AExpandedMacro()
    {
        // Constructor
    }

    public AExpandedMacro(
        @SuppressWarnings("hiding") TString _name_,
        @SuppressWarnings("hiding") List<TString> _params_,
        @SuppressWarnings("hiding") List<TString> _ancestorRefs_,
        @SuppressWarnings("hiding") List<TString> _expands_)
    {
        // Constructor
        setName(_name_);

        setParams(_params_);

        setAncestorRefs(_ancestorRefs_);

        setExpands(_expands_);

    }

    @Override
    public Object clone()
    {
        return new AExpandedMacro(
            cloneNode(this._name_),
            cloneList(this._params_),
            cloneList(this._ancestorRefs_),
            cloneList(this._expands_));
    }

    public void apply(Switch sw)
    {
        ((Analysis) sw).caseAExpandedMacro(this);
    }

    public TString getName()
    {
        return this._name_;
    }

    public void setName(TString node)
    {
        if(this._name_ != null)
        {
            this._name_.parent(null);
        }

        if(node != null)
        {
            if(node.parent() != null)
            {
                node.parent().removeChild(node);
            }

            node.parent(this);
        }

        this._name_ = node;
    }

    public LinkedList<TString> getParams()
    {
        return this._params_;
    }

    public void setParams(List<TString> list)
    {
        this._params_.clear();
        this._params_.addAll(list);
        for(TString e : list)
        {
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
        }
    }

    public LinkedList<TString> getAncestorRefs()
    {
        return this._ancestorRefs_;
    }

    public void setAncestorRefs(List<TString> list)
    {
        this._ancestorRefs_.clear();
        this._ancestorRefs_.addAll(list);
        for(TString e : list)
        {
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
        }
    }

    public LinkedList<TString> getExpands()
    {
        return this._expands_;
    }

    public void setExpands(List<TString> list)
    {
        this._expands_.clear();
        this._expands_.addAll(list);
        for(TString e : list)
        {
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._name_)
            + toString(this._params_)
            + toString(this._ancestorRefs_)
            + toString(this._expands_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._name_ == child)
        {
            this._name_ = null;
            return;
        }

        if(this._params_.remove(child))
        {
            return;
        }

        if(this._ancestorRefs_.remove(child))
        {
            return;
        }

        if(this._expands_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        if(this._name_ == oldChild)
        {
            setName((TString) newChild);
            return;
        }

        for(ListIterator<TString> i = this._params_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((TString) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        for(ListIterator<TString> i = this._ancestorRefs_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((TString) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        for(ListIterator<TString> i = this._expands_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((TString) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
