package MachineLearningChallenge;
import java.util.ArrayList;


public class Solution {
	/**
	 * will be called once with two datasets of 500 paths. 
	 * Each path contains 100 positions.
	 * @return
	 */
	public void train(ArrayList<ArrayList<Float>> samplesDrunk, ArrayList<ArrayList<Float>> samplesSober){
		System.out.println("I should do something... right?");
	}
				

	/**
	 * return true if you give it a drunk guy's path, false otherwise
	 * Will be called once automatically when you run SolutionTest after train, so you know it is called correctly and work as expected.
	 * we STRONGLY suggest you to test more than one sample to know if your suggestions work ;)	
	 * @return
	 */
	 public boolean evaluate(ArrayList<Float> sample){
		 //You're always drunk, just admit it.
		 return true;
	 }
}
